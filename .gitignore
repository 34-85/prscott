# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local

# Env
.env
.env.*
!.env.example
# .env.production holds only the Supabase URL + publishable key, which are
# public by design (they ship in the client bundle; RLS protects the data).
!.env.production

# TS build info
*.tsbuildinfo
